<%= @note.id ? 'Edit Note' : 'Create Note' %>

<%= form_for @note do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name, required: true %>
    <%= f.label :body %>
    <%= f.text_area :body %>
    <%= f.submit %>

<% end %>